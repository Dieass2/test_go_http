server:
  port: 8081
  read_timeout: 10s
  write_timeout: 10s

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: subscriptions
  max_connections: 25
  max_idle_connections: 5

logging:
  level: info
  format: json
